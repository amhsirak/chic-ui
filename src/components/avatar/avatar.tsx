import styled from 'styled-components';

interface AvatarProps {
    src: string;
    className?: string;
    width?: string;
    height?: string;
    name?: string;
    hasBadge?: boolean;
    isActive?: boolean;
}